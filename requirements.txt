# SLATE runtime dependencies — pinned to the versions the system runs on.
# Python: 3.14 (uv-managed interpreter, /Users/gjw255/.local/bin/python3).
# Install:  pip install -r requirements.txt   (or:  uv pip install -r requirements.txt)
numpy==2.4.0
pandas==2.3.3
anthropic==0.79.0
fastapi==0.135.3
uvicorn==0.43.0
pyarrow==25.0.0
requests==2.32.5
httpx==0.28.1
scikit-learn==1.8.0
backoff>=2.0.0
